import DisponibilidadEditor from "@/components/disponibilidad/DisponibilidadEditor";

export const metadata = { title: "Disponibilidad — ProfeLink" };

export default function DisponibilidadPage() {
  return (
    <div>
      <DisponibilidadEditor />
    </div>
  );
}
